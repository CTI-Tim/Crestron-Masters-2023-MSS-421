namespace SimplWindowsCWSIntegration
{
    /// <summary>
    /// Event ids for the volume buttons.
    /// These mirror a set of matching constants in Simpl+.
    /// </summary>
    internal enum VolumeEventId
    {
        up = 3,
        down = 4,
        raise = up,
        lower = down,
    }
}